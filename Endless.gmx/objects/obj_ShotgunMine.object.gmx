<!--This Document is generated by GameMaker, if you edit it by hand then you do so at your own risk!-->
<object>
  <spriteName>spr_ShotgunMine</spriteName>
  <solid>0</solid>
  <visible>-1</visible>
  <depth>0</depth>
  <persistent>0</persistent>
  <parentName>&lt;undefined&gt;</parentName>
  <maskName>&lt;undefined&gt;</maskName>
  <events>
    <event eventtype="3" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>var en;
en=instance_nearest(x,y,obj_Enemy_Parent)
if en!=noone
{
    if distance_to_object(en)&lt;=50
    {
        var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle
        image_angle = direction
        //etc
    }
        
    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 10
        image_angle = direction
        //etc
    }
        
    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 20
        image_angle = direction           
        //etc
    }
        
     var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 30
        image_angle = direction
        //etc
    }
    
     var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 40
        image_angle = direction
        //etc
    }
    
     var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 50
        image_angle = direction
        //etc
    }
    
    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 60
        image_angle = direction
        //etc
    }
    
    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 70
        image_angle = direction
        //etc
    }
    
    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 80
        image_angle = direction
        //etc
    }
    
    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 90
        image_angle = direction
        //etc
    }
    
    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 100
        image_angle = direction
        //etc
    }
    
    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 110
        image_angle = direction
        //etc
    }
    
    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 120
        image_angle = direction
        //etc
    }
    
    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 130
        image_angle = direction
        //etc
    }
    
    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 140
        image_angle = direction
        //etc
    }
    
    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 150
        image_angle = direction
        //etc
    }
    
    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 160
        image_angle = direction
        //etc
    }
    
    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 170
        image_angle = direction
        //etc
    }
    
    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 180
        image_angle = direction
        //etc
    }
    
    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 190
        image_angle = direction
        //etc
    }
    
    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 200
        image_angle = direction
        //etc
    }
    
    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 210
        image_angle = direction
        //etc
    }
    
    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 220
        image_angle = direction
        //etc
    }

    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 230
        image_angle = direction
        //etc
    }

    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 240
        image_angle = direction
        //etc
    }

    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 250
        image_angle = direction
        //etc
    }

    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 260
        image_angle = direction
        //etc
    }

    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 270
        image_angle = direction
        //etc
    }

    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 280
        image_angle = direction
        //etc
    }

    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 290
        image_angle = direction
        //etc
    }

    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 300
        image_angle = direction
        //etc
    }

    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 310
        image_angle = direction
        //etc
    }

    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 320
        image_angle = direction
        //etc
    }

    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 330
        image_angle = direction
        //etc
    }

    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 340
        image_angle = direction
        //etc
    }

    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 350
        image_angle = direction
        //etc
    }

    var obj = instance_create(x, y,obj_ShotgunMineBullet); //create moving bullet towards facing area
    with(obj) 
    {
        speed = 40;
        direction = image_angle + 360
        image_angle = direction
        //etc
    }
    
    audio_play_sound(snd_ShotyFireandLoad,1,false)
    instance_destroy()
    }
    //Try this for through walls
    /*if collision_line(x,y,en.x,en.y,obj_WallParent,false,true) 
    {
        CantSeeYou = 1;
    }*/
}
</string>
          </argument>
        </arguments>
      </action>
    </event>
  </events>
  <PhysicsObject>0</PhysicsObject>
  <PhysicsObjectSensor>0</PhysicsObjectSensor>
  <PhysicsObjectShape>0</PhysicsObjectShape>
  <PhysicsObjectDensity>0.5</PhysicsObjectDensity>
  <PhysicsObjectRestitution>0.100000001490116</PhysicsObjectRestitution>
  <PhysicsObjectGroup>0</PhysicsObjectGroup>
  <PhysicsObjectLinearDamping>0.100000001490116</PhysicsObjectLinearDamping>
  <PhysicsObjectAngularDamping>0.100000001490116</PhysicsObjectAngularDamping>
  <PhysicsObjectFriction>0.200000002980232</PhysicsObjectFriction>
  <PhysicsObjectAwake>-1</PhysicsObjectAwake>
  <PhysicsObjectKinematic>0</PhysicsObjectKinematic>
  <PhysicsShapePoints/>
</object>
